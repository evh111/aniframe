# This is just some test data that's useful for debugging.

import numpy as np
from devices.pixel import Pixel

#red_square_3x3 = np.full((3,3), Pixel(1,0,0))

#blue_square_4x4 = np.full((4, 4), Pixel(0,0,1))
